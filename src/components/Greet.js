import  React  from 'react';

const Greet = ({name, age, genre}) =>{
    // const state = this.state;
    return <h2> Greet Tag: Hello {name}! you're {age} years old. {genre}</h2>;
}

export default Greet;
